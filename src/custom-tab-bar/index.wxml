<view class="tab-bar {{themeClass}}">
  <view
    wx:for="{{list}}"
    wx:key="index"
    class="tab-bar-item {{selected === index ? 'active' : ''}}"
    data-index="{{index}}"
    bindtap="switchTab"
  >
    <text class="tab-text">{{item.text}}</text>
  </view>
</view>
